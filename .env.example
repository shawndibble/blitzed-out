# Google Cloud Text-to-Speech Configuration
# Copy this file to .env and fill in your values

# Google Cloud Project ID
GOOGLE_CLOUD_PROJECT_ID=your-project-id

# Path to Google Cloud service account key file (optional)
# If not provided, will use default authentication
GOOGLE_CLOUD_KEY_FILE=./path/to/service-account-key.json

# OR use service account key as environment variable
GOOGLE_APPLICATION_CREDENTIALS=./path/to/service-account-key.json

# Optional: Base URL for TTS API (if using different endpoint)
REACT_APP_TTS_API_BASE_URL=/api/tts